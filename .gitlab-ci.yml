stages:
  - build
  - test
  - deploy

image: node:lts

.template test: &script_test
  script:
    - yarn test

build job:
  stage: build
  script:
    - yarn install
    - yarn build
  artifacts:
    paths:
      - build/
      - node_modules

test artifacts:
  stage: test
  <<: *script_test